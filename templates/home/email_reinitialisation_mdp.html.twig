{% extends 'email.html.twig' %}

{% block body %} 

  <div class="card rounded-3 mb-3 p-5">

    <div class="text-center text-white  p-3 fw-bold mt-5 mb-3 fs-5" style="background-color: #32CD32">
        <i class="fa-solid fa-utensils text-white"></i>
        <span style="color: yellow">{{ restaurant.nom }}</span> <br>
        {{ restaurant.adresse }} <br>
        {{ restaurant.code }} {{ restaurant.ville }}
    </div>

    <h1>Bonjour</h1>

	<p>Pour réinitialiser votre mot de passe, veuillez consulter le lien suivant : </p>

	<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

	<p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

	<p>A bientôt !</p>

    <div class="text-center text-white  p-3  fw-bold mb-5 fs-5" style="background-color: #32CD32">
        © 2024 Copyright - <i class="fa-solid fa-utensils text-white"></i>
        <span style="color: yellow">{{ restaurant.nom }}</span>
    </div>

  </div>

{% endblock %}